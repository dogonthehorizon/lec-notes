
\sektion{3.2}{Determinants -- Class Lecture}
\subsektion{Stuff}
\begin{proof}Let $A,B$ be an $n\times n$ matrix. Then det($AB$) = det($A$)det($B$). If det($A$) or det($B$) = $0$, then one of $A$ or $B$ has linearly independent rows and columns.
\end{proof}